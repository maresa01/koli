# Force Railway/Nixpacks to run this as a frontend service.
# Build -> Vite build output in dist/
# Start -> serve the built app on $PORT

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

